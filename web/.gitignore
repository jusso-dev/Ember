node_modules/
.next/
out/
next-env.d.ts
.env*.local
# lib/types/ is generated by `cargo test -p ember-shared` but committed so
# CI `pnpm build` works without a Rust step in the web job.
tsconfig.tsbuildinfo
